---
import ErrorBasicLayout from "@layouts/ErrorBasicLayout.astro";
const pageTitle: string = "404";
const currentLocale = Astro.currentLocale || 'es';

const base_url = `${import.meta.env.BASE_URL}`;
---

<ErrorBasicLayout pageTitle={pageTitle} currentLocale={currentLocale}>
  <main>
    <section>
      <div class="container">
        <hgroup>
          <h1>{pageTitle}</h1>
          <p>Lo sentimos, esta página no existe.</p>
        </hgroup>
      </div>
    </section>
    <section>
      <div class="container">
        <a href={`${base_url}${currentLocale}/`} role="button">De vuelta</a>
      </div>
    </section>
  </main>
</ErrorBasicLayout>

<style>
  main {
    padding: 4rem 0;
  }
</style>
